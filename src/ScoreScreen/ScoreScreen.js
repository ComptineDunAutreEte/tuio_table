/* eslint-disable */

class ScoreScreen {
    // modal pour afficher les highscores
    constructor(pid, scores){
        this.pid = pid;
        this.scores = scores;
    }

    populate(){
        // gonfle les cartes
        const PID = "#" + this.pid;

    }

} export default ScoreScreen;